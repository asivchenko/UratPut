🌱 Что такое Greenplum DB?
 
Greenplum DB — это масштабируемая аналитическая СУБД, предназначенная для работы с большими объемами данных (Big Data).
Она основана на PostgreSQL, но сильно модифицирована для параллельной обработки и хранения больших наборов данных.
 
Её используют в основном как хранилище данных (Data Warehouse) для OLAP-нагрузок, сложных аналитических запросов и обработки petabyte-scale данных.
⚙ Основные характеристики Greenplum DB
Характеристика    Описание
🐘 Основа    PostgreSQL (fork)
🏗 Архитектура    MPP (Massively Parallel Processing)
🚀 Масштабируемость    Распределение данных по множеству узлов
🔍 Оптимизация    Для сложных аналитических SELECT (OLAP)
💽 Поддержка данных    Таблицы, партиции, внешние таблицы (External Tables)
📊 Интеграция    С Hadoop через PXF, с Kafka и др.
🔄 Тип нагрузки    Read-heavy (большие JOIN / GROUP BY / агрегаты)
🏗 Архитектура Greenplum
 
Greenplum — MPP база данных, т.е. данные делятся и хранятся на множестве узлов (segments).
Основные компоненты:
 
    Master
    Принимает SQL-запросы пользователей и строит план их выполнения.
 
    Segments
    Это отдельные инстансы PostgreSQL, расположенные на разных серверах.
    На них физически хранятся данные.
 
    Interconnect
    Сеть, через которую Master и сегменты обмениваются данными.
 
🚀 Почему Greenplum для DWH?
 
    Данные шардируются (distributed tables) — каждая таблица разбивается по сегментам. Запросы выполняются параллельно на всех сегментах.
 
    Поддержка партиций для удобного управления большими таблицами по дате, региону и т.д.
 
    Внешние таблицы позволяют читать данные напрямую из файлов (CSV, Hive, Hadoop HDFS и др.).
 
    Хорошая работа с массивами, JSON, благодаря наследованию фич PostgreSQL.
 
    Advanced Optimizer (ORCA) для построения оптимальных планов запросов.
 
📌 Ограничения
 
    Не для OLTP:
    Высокая параллельная вставка / обновления — не её сценарий. Лучше подходит для больших batch загрузок.
 
    Требует продуманной схемы распределения (DISTRIBUTED BY) — чтобы запросы не приводили к shuffle по сети.
 
🚀 Пример простого создания таблицы
 
CREATE TABLE sales (
    sale_id BIGINT,
    customer_id BIGINT,
    product_id BIGINT,
    sale_date DATE,
    amount NUMERIC
)
DISTRIBUTED BY (customer_id)
PARTITION BY RANGE(sale_date)
(
    START ('2025-01-01') INCLUSIVE
    END ('2026-01-01') EXCLUSIVE
    EVERY (INTERVAL '1 month')
);
 
📊 Для чего используют Greenplum?
 
✅ Построение корпоративных DWH.
✅ Сложные BI-отчёты с множеством JOIN и GROUP BY.
✅ Машинное обучение на SQL (есть встроенные библиотеки madlib).
✅ Data Science пайплайны, когда данные уже в Greenplum.
💬 Заключение
 
Greenplum DB — мощное open source MPP решение на базе PostgreSQL, которое позволяет обрабатывать огромные объемы данных с помощью параллельных вычислений.
Используется в роли высокопроизводительных аналитических платформ, ETL-стейджингов и корпоративных DWH.


🏗️ 1. Порождающие шаблоны (Creational Patterns)
 
Они управляют созданием объектов, отделяя это от кода, который их использует.
Шаблон    Описание    Пример применения
Singleton    Гарантирует, что у класса есть только один экземпляр, и предоставляет к нему глобальную точку доступа.    Менеджер соединений с БД.
Factory Method    Определяет интерфейс для создания объекта, но позволяет подклассам решать, какой класс инстанцировать.    Создание разных видов документов.
Abstract Factory    Предоставляет интерфейс для создания семейств взаимосвязанных объектов без указания их конкретных классов.    GUI для разных ОС.
Builder    Отделяет конструирование сложного объекта от его представления, так что один и тот же процесс может создавать разные представления.    Строитель HTML / PDF документа.
Prototype    Создает новые объекты путем клонирования существующих.    Копирование сложных объектов.
🔄 2. Структурные шаблоны (Structural Patterns)
 
Они определяют, как классы и объекты могут быть составлены в более крупные структуры.
Шаблон    Описание    Пример применения
Adapter    Позволяет объектам с несовместимыми интерфейсами работать вместе.    Адаптер старого API под новый интерфейс.
Bridge    Отделяет абстракцию от реализации, позволяя изменять их независимо.    Рисование фигур с разными библиотеками рендеринга.
Composite    Объединяет объекты в древовидные структуры для представления иерархий часть-целое.    Меню и подменю.
Decorator    Динамически добавляет объектам новые обязанности.    Добавление логирования или кэширования.
Facade    Предоставляет упрощенный интерфейс к набору интерфейсов в подсистеме.    Подсистема для работы с медиа: звук, видео, субтитры.
Flyweight    Позволяет эффективно поддерживать множество мелких объектов, разделяя общее состояние.    Текстовый редактор: символы с общим стилем.
Proxy    Заместитель другого объекта для контроля доступа к нему.    Ленивая загрузка изображений.

🚦 3. Поведенческие шаблоны (Behavioral Patterns)
 
Определяют, как объекты взаимодействуют и как распределяются обязанности.
Шаблон    Описание    Пример применения
Chain of Responsibility    Передает запрос вдоль цепочки обработчиков.    Обработка HTTP-запросов через фильтры.
Command    Заворачивает запрос в объект, позволяя параметризовать клиентов.    Отмена / повтор действия.
Interpreter    Для определения грамматики и интерпретации предложений.    Интерпретация выражений в калькуляторе.
Iterator    Последовательный доступ к элементам коллекции без раскрытия ее внутренней структуры.    Цикл по коллекции.
Mediator    Определяет объект, который централизует коммуникацию между объектами.    GUI компоненты сообщают через посредника.
Memento    Сохраняет и восстанавливает предыдущее состояние объекта.    Undo в текстовом редакторе.
Observer    Автоматически уведомляет зависимые объекты об изменениях.    Система событий и подписчиков.
State    Позволяет объекту менять свое поведение при изменении внутреннего состояния.    Автомат с напитками.
Strategy    Определяет семейство алгоритмов, инкапсулирует их и делает взаимозаменяемыми.    Сортировка с выбором алгоритма.
Template Method    Определяет структуру алгоритма, позволяя подклассам переопределять отдельные шаги.    Алгоритм оплаты с различными методами.
Visitor    Добавляет операции к объектам, не изменяя их классы.    Обход AST в компиляторе.
📌 Пример кода — Singleton
 
public class Singleton {
    private static Singleton instance;
 
    private Singleton() {}
 
    public static Singleton getInstance() {
        if (instance == null) {
            instance = new Singleton();
        }
        return instance;
    }
}
 
--
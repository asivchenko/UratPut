Отличный вопрос! Часто возникает удивление: почему Oracle выбирает полное сканирование таблицы (FULL TABLE SCAN), хотя индекс есть? Это поведение может быть вполне обоснованным.

Вот основные причины, почему Oracle предпочитает FULL SCAN вместо INDEX SCAN:
🔹 1. Низкая селективность условия

Если условие WHERE возвращает большую часть строк таблицы, то:

    Индексное сканирование → много обращений к таблице (table access by ROWID).

    Полное сканирование → быстрее, т.к. читается блоками подряд.

Пример:

SELECT * FROM orders WHERE status = 'ACTIVE';

Если 90% заказов — "ACTIVE", индекс не поможет.
🔹 2. Отсутствие нужного индекса

Индекс может:

    Не включать нужную колонку.

    Быть по другой колонке.

    Не покрывать все нужные данные (не covering index).

🔹 3. Функции в условии WHERE

WHERE UPPER(last_name) = 'IVANOV'

→ не может использовать обычный индекс last_name.

✔ Решение: function-based index.
🔹 4. Плохая или устаревшая статистика

Если статистика неактуальна (DBMS_STATS давно не запускался), Oracle может неправильно оценить селективность и выбрать suboptimal plan.
🔹 5. Использование bind-переменных (bind peeking)

Когда используешь переменные (:status), Oracle на первом выполнении "угадывает" селективность, но может выбрать неподходящий план для последующих значений.
🔹 6. Требуется доступ к большинству колонок

Если нужны почти все поля таблицы:

    При использовании индекса всё равно нужно идти за данными в таблицу (rowid lookup).

    FULL SCAN выгоднее — читаем сразу всё.

🔹 7. Параллельное выполнение (Parallel Execution)

Полное сканирование может быть выбрано, если Oracle решил использовать параллелизм:

/*+ PARALLEL(orders 8) */

FULL SCAN легче распараллелить, чем INDEX SCAN.
🔹 8. Hint или фиксированный план

    Хинт /*+ FULL(t) */ принуждает к полному сканированию.

    SQL Plan Baseline может "зафиксировать" plan с полным сканированием.

🔹 9. Индекс фрагментирован или неэффективен

Если:

    Индекс сильно фрагментирован.

    Статистика на индексе отсутствует.

    Индекс больше по объему, чем таблица.

→ Oracle может сознательно его игнорировать.
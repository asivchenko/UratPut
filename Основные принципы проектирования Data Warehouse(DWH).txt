Основные принципы проектирования Data Warehouse(DWH) 
1️⃣ Интеграция
 
    DWH собирает данные из разных источников (OLTP системы, CSV, API и т.д.).
 
    Не важно, откуда пришли данные — в хранилище они приводятся к единому формату, структуре, кодировкам (например, справочники с одинаковыми ключами).
 
    Это делает возможным анализ всех данных вместе.
 
2️⃣ Предметная ориентация (Subject-Oriented)
 
    Хранилище проектируется вокруг предметных областей бизнеса: продажи, финансы, запасы, клиенты.
 
    Каждая часть хранилища посвящена конкретной теме, а не операциям, как в OLTP.
 
3️⃣ Временная вариативность (Time-variant)
 
    DWH хранит данные в историческом разрезе.
 
    Например, можно узнать продажи товара за любой период последних 5 лет.
 
    Данные обычно имеют поля valid_from / valid_to или просто дату snapshot.
 
4️⃣ Неволатильность (Non-volatile)
 
    После загрузки данные не изменяются и не удаляются, только добавляются новые.
 
    Обновления происходят пакетной загрузкой (ETL/ELT).
 
    Это важно для исторического анализа (данные вчера не должны «переписаться»).
 
5️⃣ Оптимизация под чтение (Read-optimized)
 
    Хранилища данных проектируют для быстрой агрегации и аналитических запросов (OLAP), а не для транзакций (INSERT/UPDATE).
 
    Часто используются денормализованные модели (звезда, снежинка), индексы, агрегационные таблицы, OLAP-кубы.
 
⚙ Модели проектирования
⭐ Звезда (Star schema)
 
    Факт (таблица фактов) в центре: хранит события, транзакции (sales, orders).
 
    Вокруг — измерения (dimension tables): клиенты, товары, магазины, время.
 
    Просто, быстро работает для аналитики.
 
❄ Снежинка (Snowflake schema)
 
    Более нормализованная: измерения разбиваются на подтаблицы.
 
    Экономит место, но усложняет запросы.
 
🛠 Принципы ETL/ELT
 
    Данные экстрагируются (Extract) из источников.
 
    Преобразуются (Transform) — очищаются, обогащаются, приводятся к единым справочникам и кодам.
 
    Загружаются (Load) в DWH.
 
    Иногда используется ELT (трансформации происходят уже в хранилище, например в Snowflake, BigQuery).
 
🚦 Другие принципы хорошего проектирования
 
✅ Стабильность схемы (schema-on-write).
✅ Логическая независимость от источников.
✅ Согласованность бизнес-ключей и справочников.
✅ Метаданные для управления данными (data lineage, data catalog).
✅ Поддержка медленно изменяющихся измерений (SCD Type 1, 2, 3).
📈 Главная цель
 
    Сделать данные готовыми для аналитики и бизнес-отчетов, быстро и надёжно, сохраняя их историю и контекст.
 
